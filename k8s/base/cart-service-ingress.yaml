apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: cart-service-ingress
spec:
  rules:
    - host: "shopbuddy.com"
      http:
        paths:
          - pathType: Prefix
            path: "/cart"
            backend:
              service:
                name: cart-service
                port:
                  number: 8080
