apiVersion: v1
kind: Service
metadata:
  name: k8s-dashboard-service
  labels:
    app: k8s-dashboard
spec:
  type: ClusterIP
  selector:
    app: k8s-dashboard
  ports:
    - port: 80
      targetPort: 8080
