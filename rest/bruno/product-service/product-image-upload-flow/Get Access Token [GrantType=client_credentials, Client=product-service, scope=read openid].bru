meta {
  name: Get Access Token [GrantType=client_credentials, Client=product-service, scope=read openid]
  type: http
  seq: 1
}

post {
  url: {{auth_url}}/oauth2/token
  body: formUrlEncoded
  auth: basic
}

auth:basic {
  username: {{client_id}}
  password: {{client_secret}}
}

body:form-urlencoded {
  grant_type: client_credentials
  scope: read openid
}

script:post-response {
  bru.setVar("access_token", res.body.access_token);
}

settings {
  encodeUrl: true
  timeout: 0
}
